'use client';

import { Skeleton, Spinner, Textarea } from '@nextui-org/react';

export const Home = () => {
	return (
		<div className=' w-full'>
			<div className='flex justify-center'>
      <Spinner />
      </div>
		</div>
	);
};
